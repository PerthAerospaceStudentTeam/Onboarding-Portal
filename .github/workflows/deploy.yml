name: Deploy app changes

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'frontend/**'
      - 'backend/**'

permissions:
  id-token: write
  contents: read

jobs:
  deploy-backend:
    runs-on: ubuntu-latest
    environment: prod
    steps:
      - uses: actions/checkout@v4

      - name: Azure Login (OIDC)
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Ensure containerapp CLI extension
        run: az extension add --name containerapp --upgrade

      - name: Log in to ACR
        run: az acr login --name onboardingportalprodacr

      - name: Build and push backend image
        run: |
          docker build -t onboardingportalprodacr.azurecr.io/backend:${{ github.sha }} ./backend
          docker push onboardingportalprodacr.azurecr.io/backend:${{ github.sha }}

      - name: Update Container App to new image
        run: |
          az containerapp update \
            --name onboarding-portal-prod-backend \
            --resource-group rg-portal-prod \
            --image onboardingportalprodacr.azurecr.io/backend:${{ github.sha }}

  deploy-frontend:
    runs-on: ubuntu-latest
    environment: prod
    steps:
      - uses: actions/checkout@v4

      - name: Deploy to Static Web App
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.SWA_DEPLOYMENT_TOKEN }}
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          action: upload
          app_location: /frontend
          output_location: dist
